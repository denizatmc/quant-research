# Pinned-ish core requirements for a plain `pip install -r requirements.txt`.
# For a proper editable install with extras, prefer:  pip install -e ".[deep,dev]"
numpy>=1.24
pandas>=2.0
polars>=0.20
scipy>=1.10
scikit-learn>=1.3
statsmodels>=0.14
arch>=6.0
yfinance>=0.2
SQLAlchemy>=2.0
matplotlib>=3.7
pyyaml>=6.0
tqdm>=4.65

# Optional
# torch>=2.0
# psycopg2-binary>=2.9
